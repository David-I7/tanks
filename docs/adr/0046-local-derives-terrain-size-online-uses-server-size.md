# Local derives terrain size and online uses server size

Local gameplay derives its initial terrain size from the game viewport because the client owns local map setup. Online gameplay uses the world and terrain dimensions supplied by the server because the server owns the authoritative map for a game session.
