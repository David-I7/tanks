# Shared root domain docs

The client and server are part of one Tanks Game product context, so shared glossary terms and architectural decisions live at the repository root in `CONTEXT.md` and `docs/adr/`. Existing client-local docs are treated as historical evidence to migrate selectively, not as a source of truth to copy wholesale.
