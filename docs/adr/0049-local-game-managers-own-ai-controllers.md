# Local game managers own AI controllers

AI controllers belong to local gameplay management rather than the game engine. The game engine handles browser input and rendering, while local game managers decide when AI-controlled players should produce game actions and submit those actions to the simulation manager.
