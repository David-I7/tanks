# Reference render assets by stable IDs

Online protocol messages reference render assets through stable IDs instead of server-provided asset paths, CSS classes, or bundled filenames. The server stays authoritative over gameplay definitions and outcomes, while the client maps IDs to local images, animations, styles, and sounds appropriate to its rendering implementation.
