# Version online gameplay definitions

Online Game Sessions carry a Gameplay Definition Version so clients can verify that their render mappings and protocol expectations match the server-owned rules data. A mismatch should fail clearly or force a refresh rather than allowing the client to render unknown tanks, projectiles, terrain effects, or impact animations incorrectly.
